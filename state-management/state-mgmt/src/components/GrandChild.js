// IMPORT REACT
import React from "react";

// ADDITIONAL IMPORTS

// CREATE COMPONENT
const GrandChild = ({ data }) => {
  return (
    <div>
      <h1>{data}</h1>
    </div>
  );
};

// EXPORT COMPONENT
export default GrandChild;